# This site's identity, for `.devtools/rebuild-from-recipes`.
#
# main leaves the name neutral because it holds only what the serverless
# template can take upstream. A site built on it names itself here.
SITE_NAME=Deciphered
